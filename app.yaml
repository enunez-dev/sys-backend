name: sys-backend-app
services:
  - name: sys-backend
    image:
      registry: registry.digitalocean.com/dev-registry-2025/sys-backend:1.0.0
      registry_type: DOCR
    envs:
      - key: NODE_ENV
        value: production
    health_check:
      http_path: /health
